services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ticket
      POSTGRES_PASSWORD: ticket
      POSTGRES_DB: ticket
    ports:
      - "5432:5432"
    volumes:
      - ticket_pg:/var/lib/postgresql/data

volumes:
  ticket_pg:
